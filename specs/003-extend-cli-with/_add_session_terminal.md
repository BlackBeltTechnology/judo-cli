Currently the logs presented in frontend terminal. I would like to add a Button called 'JUDO Terminal'. Whenit pushed the xterm console have to be cleared and have to change xterminal to an interactive terminal which behaves same as 'judo session' command. So it have to be a two way socket connection where I can give commands and response it to console. The very same commands can be used as in 'judo session'. When switch back to logs clearing the xterm terminal and showing the logs again. When switch back to session return the interactive terminal. So keep the interactive terminal state as it is when switch back and forth.
